services:
  reports-enricher:
    platform: linux/arm64
    image: ${DOCKER_IMAGE_NAME:-ghcr.io/liante0904/ssh-reports-enricher}:${APP_ENV:-prod}
    container_name: ssh-reports-enricher-${APP_ENV:-prod}
    build:
      context: .
    restart: unless-stopped
    network_mode: "host"
    env_file:
      - .env
    volumes:
      - ~/logs:/log
    environment:
      - APP_ENV=${APP_ENV:-prod}
      - TZ=Asia/Seoul
      - LOG_BASE_DIR=/log
      # 신규 INSERT 주기와 맞춘 저빈도 안전망. env_file의 구형 30초 값보다 우선한다.
      - ENRICHER_INTERVAL_SECONDS=1800
      - ENRICHER_BATCH_LIMIT=${ENRICHER_BATCH_LIMIT:-100}
    stdin_open: true
    tty: true
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
